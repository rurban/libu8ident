#include <assert.h>
#include <math.h>

int main() {
  int ℌ = 1; // which is which?
  int Ⅸ = 2;
  assert(H == 1);   // same NFKC as ℌ
  assert(IX == 2);  // same NFKC as Ⅸ
  double ℯ = M_E;
  assert(e == M_E); // same NFKC as ℯ
  int ℨ = 3;
  assert(Z == 3); // same NFKC as ℨ
  return 0;
}
